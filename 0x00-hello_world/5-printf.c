#include <stdio.h>
/**
 * main - This program uses the printf function to output a sentence
 * Return: 0 (It returns Zero upon success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
